[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  # Vite 8 requires Node ^20.19.0 || >=22.12.0. Pin to 20 (matches the passing
  # GitHub Actions build); Netlify's default is older and fails the build.
  NODE_VERSION = "20"
